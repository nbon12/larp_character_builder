@page
@model LarpCharacterBuilder3.PageModels.Character.IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Build your LARP character here.</p>
</div>

<h2>All Characters</h2>
<form method="post">
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var character in Model.Characters)
            {
                <tr>
                    <td> @character.Id  </td>
                    <td>@character.Name</td>
                    <td>
                        <a asp-page="./CharacterSheet" asp-route-id="@character.Id">Edit</a> |
                        <button type="submit" asp-page-handler="delete"
                                asp-route-id="@character.Id">delete
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <a asp-page="Create">Create New</a>
</form>
